local ok, persisted = lk.safe_require("persisted")
if not ok then
  return
end

----------------------------------------------------------------------
-- NOTE: setup {{{
----------------------------------------------------------------------
persisted.setup({
  use_git_branch = true,
  autoload = true,
})
-- }}}
----------------------------------------------------------------------

----------------------------------------------------------------------
-- NOTE: telescope extension {{{
----------------------------------------------------------------------
require("telescope").load_extension("persisted")
-- }}}
----------------------------------------------------------------------

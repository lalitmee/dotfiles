---
description: Check the status of the dotfiles repo
agent: run
---

## Mission: Dotfiles Status

Your task is to report the current status of the dotfiles repository.

### Workflow:

1.  **Git Status**:

    - Navigate to `~/dotfiles`.
    - Run `git status` to show working tree status.
    - Run `git log -1` to show the latest commit.

2.  **Report**:
    - Summarize the status for the user (e.g., "Clean", "Modified files", "Behind remote").

### QUESTION:

$ARGUMENTS

# key: rfc
# name: react functional component
# --
import React from 'react'

function ${1:Component} (${2:props}) {
    return (
        <div>
            ${3:children}
        </div>
    )
}

export default ${1}
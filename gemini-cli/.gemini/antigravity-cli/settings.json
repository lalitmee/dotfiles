{
  "allowNonWorkspaceAccess": true,
  "colorScheme": "tokyo night",
  "model": "Gemini 3.5 Flash (Medium)",
  "permissions": {
    "allow": [
      "command(git log)",
      "command(git diff)",
      "command(git status)",
      "command(npm list)",
      "command(npm search)",
      "command(which)",
      "command(luac)"
    ]
  },
  "trustedWorkspaces": [
    "/home/lalitmee/dotfiles"
  ]
}
